services:
  roamline:
    image: ${ROAMLINE_IMAGE:-ghcr.io/hibenji/roamline:latest}
    ports:
      - '7863:7863'
    environment:
      HOST: 0.0.0.0
      PORT: 7863
      NODE_ENV: production
    restart: unless-stopped
